# 弦音 · 家庭练琴版 PRD

更新：2026-09-09（多风格教学、创作演奏、循环变化与伴奏双轨强化）。唯一公开入口：https://rexrockya.github.io/tuner/

## 用户与目标

家长在手机上辅导孩子练琴：找到谱子、看清谱面、以稳定节拍试听和跟练。吉他练习者能够按正在练习的和声找到乐句、生成原创练习句，并使用 Blues 伴奏练习即兴。保留原有调音、吉他教学、节拍器、Jam 和账号能力，不把它改成商业识谱平台。

## 本版范围与验收

| 功能 | 验收标准 |
| --- | --- |
| 小提琴曲库 | “小提琴四级到六级”文件夹，按上传日期分组；保留 2026-09-06 的 3 份谱 |
| 删除旧谱 | 移除独立的“第一学生协奏曲 · 第一乐章”及公开资源；旧链接提示移除并返回曲库，不打开其他曲子 |
| 钢琴曲库 | 按曲风折叠：古典 7 首、拉格泰姆 3 首；古典为大众分类，包含浪漫派及印象派作品，曲目原风格标签保留 |
| 收藏与搜索 | 收藏置顶且不从乐器分类消失；搜索匹配时展开文件夹；可按“古典”找到全部古典子风格作品 |
| 手机看谱 | 默认完整适宽；双指缩放 50%–400%，单指拖动；设置中可恢复适宽 |
| 手机控制 | 竖屏、短横屏使用固定精简播放栏；设置展开才显示完整控制；旋转后折叠设置 |
| 播放跟随 | 高亮当前小节，越出视口时横向/纵向跟随；手指操作时暂缓，松手约 1.8 秒后恢复播放跟随 |
| 节拍和音源 | 音符、节拍共用音频时钟；支持正确拍号与强弱拍、独立双音量；跨乐器换谱采用对应默认音色 |
| 录入入口 | 本版不显示、不加载录入脚本；不清理用户浏览器已有原稿数据 |

## 乐谱准确性边界

第一张上传图为整页可播放校对版：35 小节、276 次音符起音、140 个四分音符拍、108 BPM。默认略过开头 6 小节休止，回到开头可以播放这些休止。

它不是已专业审核的权威谱。第 18 小节存在显式补足的半拍休止，第 25、32 小节等仍需对照原图复核。第二、三张仍是待校对转录稿。完整原图是对照材料，不提供原图逐音定位。

## 图片录入的决定

暂不建设收费识谱、公开上传服务或无人值守同步。原先入口仅保存当前浏览器的 IndexedDB 草稿，不能把手机照片传到电脑项目，因此撤下。

当前可用流程：将图片/PDF作为任务附件交给助手 → 在项目按上传日期归档 → 转录 MusicXML 和播放数据 → 标明疑点并测试 → 经用户要求发布。网页不会自动通知或唤醒助手，也不会在用户下次打开时自行读取另一台手机的数据。

以后恢复上传前必须确定：存储位置与访问权限、上传成功凭据、待处理清单、人工校对状态、失败通知、隐私和删除策略。不要将私人照片或云盘凭据放入公开仓库；不因发现某个现成设备或账号便自动接入。

## 非目标与验证边界

不重写 Android 应用，不新增收费服务，不承诺扫描谱百分之百识别正确。不为了测试创建真实账号、写入真实 Jam 内容或更改云端权限。

自动验证覆盖所有入口结构、教学交互、13 首资源、播放调度、节拍/音量、缩放与分类、可选 Worker 构建。真实 Chromium 已验证桌面、375 px 竖屏及 844 px 横屏主要操作。实际手机触感、麦克风硬件输入、真人音色听感、真实登录及 Flat 授权仍需设备验收；不能用模拟测试或浏览器尺寸模拟代替这些结论。


## 教学扩展的产品验收

教学提供课程、乐句资料库、创作、伴奏四个入口。搜索现有乐句和生成原创练习句是两个清楚区分的流程，均通过 GitHub Pages 页面使用。

| 能力 | 验收标准 |
| --- | --- |
| 和声筛选 | 接受 `2-5-1`、`2-5-1-6`、罗马级数及 `Dm7 G7 Cmaj7` 等和弦名；处理半减七、变音与跨调；非法输入明确提示，不冒充零结果 |
| 组合查找 | 和声与分类、调性、拍号、收藏条件组合；保留选句、随机、播放、波形、A/B 和掌握进度；完整索引含 2,525 条 BopLand 乐句（615 种实际和声）及 20 条 GuitarSet 音频/标注谱面 |
| 原创生成 | 静态页面按和声、调性、律动和种子生成原创练习句；TAB 弦品与播放音高一致，支持换句、点选、试听、本机收藏重现、MIDI 导出；Lead 可选择独立演奏律动、双音／Voicing、4 轮乐句／密度变化及六种非真人模仿的演奏取向；不依赖后台模型，不标为经典作品 |
| Blues 伴奏 | 标准 12 小节、Quick change、小调 Blues、自定和声；8 类律动；鼓、电贝斯、键盘、真实主奏／节奏吉他采样、辅助打击与弦乐层；6 类 Bassline 与整轮随机，独立声部混音 |
| 伴奏控制 | 变速、点选小节、单节/整轮循环、分轨音量；关闭循环在当前和声轮末停止；停止或切页取消待启动及已发声声部 |
| 音源可靠性 | 自托管采样并保留 CC0 许可和来源；优先 FLAC，解码/下载失败回退 WAV；超时可重试，成功解码结果复用；预热不发声，尊重节省流量设置 |
| 存储降级 | 禁止读取、写满或删除失败均不得中断页面启动及练习；本页暂存并提示刷新可能丢失，原创保存失败显示“本次暂存”并提示导出 MIDI；不误报持久保存成功 |

规则生成追求可练习的和弦音落点、经过音与节奏变化；真实采样和编配规则是本版音质方案。“Blues 味道正”仍需真人听感反馈，不能由音源加载成功来证明。已有乐句保留来源，谱图与示范 MP3 仍有源站网络依赖。

## 界面原则与验收

按迪特拉姆斯的有用、易理解、克制和注重细节原则精简整个网站：删除重复的 Guitar lick Library 标题、音阶训练及无助于操作的提示；次级筛选、房间功能、音量与来源按需展开。

开关使用稳定名称，例如“伴奏”，以颜色、控件状态及 `aria-pressed` 表达启停，不重复写“开/关”。精简不能移除必要标签、键盘焦点、加载/错误反馈或来源许可。375 px 下五个导航项可见，无页面横向溢出；播放期间不覆盖正在编辑的 BPM。

## 性能验收与测量口径

| 项目 | 发布门槛及记录方法 |
| --- | --- |
| 初始加载 | 默认调音页不提前加载 Tone、OSMD、fflate、完整乐句索引及隐藏媒体；HTML、初始 JS/CSS 同口径 gzip 6 资源预算不超过 120,000 B；依赖按序 defer |
| 页面交互 | 主路径无脚本错误；真实浏览器检查筛选、生成、切页和播放状态反馈；记录 Event Timing、capture→rAF、长任务。目标会话交互低于 100 ms，异常需复现分析，不把少量观察宣称为真实用户 INP |
| 热播放 | 伴奏计划首音提前量不超过 30 ms，当前 25 ms；单独记录按钮 DOM 状态就绪。调度时间、DOM 变化、网络下载与设备声学延迟分开报告 |
| 音质保留 | 12 份 FLAC/WAV 解码 PCM 一致；钢琴只减去实际乐谱及 ±12 移调用不到的采样，保留原音区和力度映射；13 份谱、19,100 次映射比较通过 |
| 持续播放 | 重复位置不重写 DOM，不抢走输入焦点；音频绝对时钟、取消和循环回归通过；性能优化不能以漏音、错误移调或后台继续播放换取 |

正式站本轮观察 LCP 964 ms、最大会话交互 64 ms，属于一次当前网络的 Chromium 记录，缓存未强制清空，不是所有设备承诺。详细环境、原始数据和剩余外部依赖见 [QA 与性能报告](notes/qa-performance-2026-09-08.md)。

## QA → 迭代 → 发布

每轮先完成功能和性能验证，再审阅报告并决定是否继续修正。启动失败、数据丢失却提示成功、播放不能停止、音符映射错误或主流程不可用属于发布阻断项，必须修复并对相关模块回归；影响基础模块时运行完整 `npm test`。

当阻断项清零、适用测试通过且验证边界明确后，先更新本 PRD、[HANDOFF](HANDOFF.md) 和 QA 报告，再精准提交、推送，等待 Pages 构建成功并验证正式站实际文件。未通过时继续这一循环，不把未验证的设备能力标成通过，也不因文档更新重新部署可选 Worker。

本次复核确认存储拒绝/写满可导致多模块启动失败，追加一轮修复；同时把 OSMD 测试切到仓库自带渲染器，缺失时明确失败，避免用本机缓存或跳过项造成假通过。发布结论及实际结果在 QA 报告更新。
自动验证覆盖所有入口结构、教学交互、13 首资源、播放调度、节拍/音量、缩放与分类、后台构建。实际手机触感、麦克风硬件输入、真人音色听感、登录及 Flat 授权仍需设备/用户验收；不能用模拟测试代替这些结论。

## 现场响度表（2026-09-07）

- 同一 GitHub Pages 入口新增「响度」标签，手机默认麦克风；不建立音频上传服务。
- 未校准显示 dBFS；采用同计权参考声级计稳定对照至少 5 秒后，显示估算 dB SPL。重开麦克风必须重校准。
- A/C 为频域近似、Z 不计权；支持 Fast/Slow、累计 Leq、SEL、Lmax、Z 采样峰值、近似三分之一倍频程频谱。频谱不等于场地传递频响，非 LUFS。
- 只累计实际收到的完整音频块；后台、输入静音/断开或连续 3 秒未收到数据时停止并保留结果。
- CSV 包含有效时长、计权读数、频谱快照、校准偏移、输入和处理状态。全部在本地生成，不录制或上传音频。

## 工具入口与两端差异化（2026-09-07）
- 顶层改为工具、教学、乐谱、Jam；工具页再选择调音器、响度表、节拍器。
- 电脑响度表提供麦克风设备选择与共享标签页/系统音轨选择，桌面指标并排展示。
- 手机采用纵向大按钮布局，提供麦克风与手机输出选项；不支持输出采集的浏览器明确禁用该项。网页不能绕过系统权限读取其他 App 音频。
- 输出音轨是数字电平，不可换算 SPL。音源切换清空结果并移除校准。


## 2026-09-08 工具与原创乐句深化验收

| 能力 | 验收行为 |
| --- | --- |
| 原创差异 | 问答、动机发展、Blues 转句、琶音、切分、留白及混合；种子可复现，旧收藏兼容，换律动/Bass 不改主旋律 |
| 吉他与伴奏 | 真实拾音器采样、双力度/双录音轮替，温暖收敛高频；独立节奏声部；Shuffle、慢 12/8、直八、Boogie、Soul、Funk、Half-time、Latin 的实际不同配器 |
| Bass | Walking、Boogie、根五、Blues riff、八度切分、持续音；随机按完整乐段选风格，不能每音随意变风格 |
| 新乐句 | 新增 20 对合法授权真人录音/原标注制谱，来源筛选和署名明确；同谱号/八度改版不充数；每次选不同乐句恢复精确原速 |
| 节目响度 | BS.1770-5 K 计权、R128 双门限 I、M/S、LRA、4×TP；累计/暂停/继续/清零/CSV；−23 LUFS 为节目参考而非现场 SPL 目标 |
| 现场声级 | 独立模式保留计权、Leq/SEL；校准仅影响估算声级，不改 LUFS/TP；内置 mic 不视为认证声级计 |
| 频谱与时间 | 对数频率曲线＋最多120秒响度轨迹；有限 Canvas 绘图，无持续后台循环；按音频样本时间累计 |
| 调音器 | 空闲音高环、无白条；440/441标准音；只在手动进入次级页时显示时间轴，单音检测、音名网格、暂停画面、清空，声音停止留空 |
| 退出行为 | 工具间导航、品牌、hash、后台/采集终止均释放相关输入或声音；迟到授权不自动开始；不自动录音或上传 |
| 性能与交付 | 大模块和媒体按需；首屏gzip6≤120,000 B；新增与原有适用回归全通过，文档/许可齐全，再按用户授权提交发布原站 |

标准算法仅支持单声道/立体声，合成标准子集和独立实现核对不等于仪表认证。停表保留最后一次显示快照（44.1/48k每4096帧约93/85ms），不是逐样本结束导出。生活声音的复音、噪声和极弱基音不保证识别；新增音色主观效果、真实 iPhone/Android 采集和解码仍需用户实机试听。


## 2026-09-08 组合练习深化

| 能力 | 验收行为 |
| --- | --- |
| 各声部风格 | 鼓7类、风琴5类、节奏吉他5类加随机经典型；风琴/节奏吉他以含None的下拉替代按钮，Bass保留原选择 |
| 收尾变化 | 每轮和声最后1–2拍各声部按选中风格填句；同轮风格连贯，随机在下轮切换，固定风格也有fill应答变化 |
| 风格独立 | 改一轨不重写别轨或Lead；整体律动约束auto鼓时间网格，显式鼓选择可有意对比 |
| 混音器 | 音色与音量一起折叠；每行清晰乐器标签、音色下拉和音量，手机不挤成逐字竖排 |
| Lead | 主奏吉他改名Lead；温暖/明亮/轻过载吉他、原乐谱大钢琴和小提琴；播放中准备完成后在小节线切换，停止时不自动播放 |
| 音色真实性 | 复用真实音色/现有采样处理；钢琴保持原音区力度，小提琴保持原延音处理，不偷换为振荡器模拟 |
| 全局谱式 | 原创默认六线谱，单一选择器切全页五线谱，实音高音谱号、休止/时值/升降号，支持点选跟随；不改音符和播放时钟 |
| 加载与保存 | 音色/谱面惰性加载，取消与旧请求隔离；保存样式/音色、旧收藏复现，Lead MIDI采用对应program |
| 脏编辑 | 编辑和声保留正在播放的旧版本；新输入须生成并在小节线提交，不能把旧旋律套上新和声 |

五线谱对旧收藏没有独立写作时值的音符作明确网格推断；演奏门长不硬写成不合理的分数拍。音色patch差异和实体手机音频效果仍需真人试听，不用合成/模拟回归代替主观音质结论。


## 2026-09-08 数字和声输入

- 创作、伴奏与乐句检索统一数字解析。英文/中文逗号分小节；同小节空格分1、2或4个和弦，平分四拍。无显式分隔时保留空格每和弦一小节；继续接受竖线、连字符、罗马数字与和弦名。
- `456456456`为九小节，移除原8位连写限制。合法数字后缀优先：`57`为五级属七，五级接七级必须写`5,7`；逗号内非法后缀不擅自拆成多个和弦。
- 数字根音以所选大调音阶为参照。裸数字保持调内三和弦，变音裸级数默认大三和弦；支持`b7/#4/♭7/♯4`。显式后缀指定性质，C调`37=E7`，小七写`3m7`。
- 用户完整示例`1maj7,57,4sus2,6aug,37,2,7dim,1`在C调为Cmaj7、G7、Fsus2、Aaug、E7、Dm、Bdim、C，八小节。
- 输入框下以原生details提供「和声怎么输入」，默认折叠；给出规则、C调示例与填入按钮。填入/编辑清除过时错误，保留当前播放；生成前不应用新和声，停止时仍须生成才可播放。默认和声/预设使用数字加逗号。
- 仍限创作/伴奏16小节、解析32和弦；字符上限512，给旧收藏明确化后的输入留足空间。新收藏独立标记harmonyVersion:2，旧v1/v2先以旧规则解析，只在含义变化时转换，保留旋律、和声、调性与时间。
- 来源库使用旧语法解析，小写b7在来源中仍为B7；用户单独b7为降七级。保持615种来源和声与原有跨调搜索覆盖。


## 2026-09-08 原创乐句演奏强度

- 「乐句」风格旁增加单一「演奏强度」下拉：轻松、标准（默认）、进阶、挑战；仅创作页显示，伴奏页隐藏。手机两列、宽屏六列，简短说明四档差异与BPM影响。
- 标准沿用原生成路径、随机序列和全部音符；轻松减少密度、正拍起音、4–8品固定把位、相邻音不超过7半音/不跨超过2根弦，仅拨弦。进阶增加八分与少量十六分；挑战增加十六分切分、音域与跨弦，但保留所选风格、动机和呼吸。
- 非标准档由同一seed的原始风格乐句派生，使用独立随机序列。滑音仅同弦相邻1–2半音，长音可揉弦，使用现有吉他播放处理。钢琴/小提琴继续演奏对应音符/节奏，不声明额外吉他技法；MIDI仍仅音高、起音、时长/力度，不导出采样滑音曲线。
- 变强度或乐句风格以原seed重新写句，BPM、音色和伴奏各轨事件保持不变；播放中在资源就绪后的小节线应用。和声已编辑未生成时只保存选择、保留旧版本播放，不能把旧旋律套上新和声。
- intensity随草稿/收藏保存，去重包含强度；旧或未知值默认为standard，旧v1算法与v2快照原样恢复。新档位最小起音间距.25拍、总数不超过256音，保留notationDuration与和弦边界。
- 密集同弦TAB每小节独立水平视口，品位最少28px间隔，键盘可进入；播放仅在当前音变化且超出视口时局部滚动，隐藏TAB不滚动；五线谱与MIDI使用同一生成音符。

## 2026-09-08 连续播放中的选项切换

- 创作与伴奏的音色、各轨风格、演奏强度、乐句风格、重新生成、和声/调/预设、收藏恢复、律动及BPM统一生成待切换版本；资源准备不改变当前音色、旋律、和声或时钟。
- 资源就绪后，在下一个尚未提前调度的四拍小节线切换声音，实际越过边界才更新谱面与播放高亮。接近小节线时若旧版本已预排，将顺延一小节，避免重复首拍或取消正在发声的音乐。
- 连续修改合并成最新完整设置；旧加载和待切换的声音均不得迟到覆盖新选择。加载失败继续旧版本，提示失败并提供重试；保存/MIDI在待切换或输入未生成时不可用，防止保存混合版本。
- 保持当前和声轮及小节位置；新和声长度改变时以新长度映射该小节，单节循环映射到有效小节。关循环且没有下一个可用小节时保留旧版本并自然结束，不自动延长或重启。
- 主动暂停、离开页面、定位或改变循环范围取消待切换版本，并恢复已经生效的选项；停止状态下编辑不自动播放。音量仍即时响应，切谱面不改变播放。
- 界面简短提示正在准备、已就绪/下一小节生效、已切换或失败重试，不逐帧重复写状态文字。保留按需采样与首屏gzip6预算。

## 2026-09-08 小提琴音量与现代吉他、电贝斯

- 创作Lead小提琴按音高采样做静态响度补偿：.08–.6秒双声道能量平均，目标-24dBFS RMS，最大增益8倍且整段峰值不超过-6dBFS；零/短源保持有限值，强峰可衰减。每个解码样本只计算一次，不改变演奏力度差、原录音文件或乐谱播放器。
- Lead与节奏吉他的warm/bright/crunch保留旧ID，升级为暖净音/亮净音/厚过载Studio处理：低切、宽带低中频、轻饱和、箱体方向高频收敛及每音柔和压缩；仍来自原24个Shinyguitar真实采样。新增节点随声音结束或候选取消释放，主混音压缩参数不变。
- Bass新增precision「P 风格 · 厚实指弹」。实际来源为CC0 Swagbass（Ibanez BTB-400QM、颈拾音器、平绕弦），P风格为声音方向，不是Fender原琴采样或背书。8根音×2独立录音、f层、4秒、44.1kHz单声道16bit，FLAC优先/WAV回退，按每对共同增益保留两次演奏电平差。
- 新Bass仅在选中且播放计划确实包含Bass时准备，不加入默认预热；并发复用、失败重试复用成功解码。播放中的切换仍准备完成后按小节线提交，旧音乐不中断。停止状态加载失败恢复前一个选项。
- 原3个Bass选项及收藏继续可用；原round/bright补充低中频厚度。P风格使用独立录音库与均衡/柔和压缩，音符按实际MIDI根音就近移调并确定性轮换RR；不改变Bassline的和声、节奏或音量滑块。
- 许可原文、原作者README、固定源提交、每文件处理与哈希完整保留。测试覆盖静态校准、lazy/fallback/cache、原播放竞态；另做离线真实PCM电平/峰值对比，不能用它冒充实体设备主观试听。

## 2026-09-08 按乐句结构自动安排音符密度

- 原创页「演奏强度」改名「音符密度」，五项：自动·疏密有致（新创作默认）、稀疏·多留白、适中·均衡、密集·流动、繁密·音群。说明聚焦用音数量、流动感与留白，不再以练习难度或技法等级命名。
- 自动以句子结构控制疏密。问答形成一繁一简并可反转先后；动机从简短核心起步，保留可辨认的材料逐渐发展，结尾收束；其他风格各自保留分解和弦、蓝调回转、切分或慢句的语汇。
- 密度起伏应反映在真实音符、休止与时值，不能只随机换档或只修改标签。保留和声边界、可播放指法、单声部和全句256音符限制；短句不为了凑复杂度而塞满。
- 新自动模式采用独立生成分支；旧easy/standard/advanced/challenge的生成与旧收藏快照保持兼容，缺失或未知保存值仍按旧standard恢复。内部intensity字段/控件ID沿用，用户界面统一音符密度，不做破坏性收藏迁移。
- 自动状态随草稿、收藏保存。更换密度/乐句风格以原seed重新写句，不重写伴奏；播放中在新资源就绪后的小节线应用，音乐继续。TAB、五线谱与MIDI使用同一最终音符。

## 2026-09-09 多风格教学、Funk 节奏练习与电子音色

| 能力 | 验收行为 |
| --- | --- |
| 六种风格 | 教学统一支持 Blues、Jazz、Funk / Soul、Shoegaze、Folk、R&B；风格切换同步更新和声预设、默认音色、旋律词汇与自动配器，并分别保存创作／伴奏草稿 |
| 分级课程 | 共 21 个原创练习；Funk / Soul 有 6 课，其中 3 课提供随播放移动的十六分节奏吉他格，可一键静音示范后自己弹。课程进度、收藏与原乐句资料库继续保留 |
| 流派生成 | 同一和声在不同流派使用不同旋律规则与声部池；流派选择必须贯穿固定、随机、指定顺序及密度随机的每一轮 Lead 生成，不能在循环变化时退回通用生成器 |
| 电子音色 | 鼓增加按需加载的原创 Electro 采样；Bass 增加 Synth／Acid，键盘增加 Synth／Pad；节奏吉他与 Lead 增加干净短奏／空间延音。合成与环境尾音服从取消、暂停、最新选择胜出和按需加载边界 |
| 兼容与入口 | 旧收藏与无流派草稿继续恢复；所有功能留在同一静态 GitHub Pages 体验中，不增加用户可见后台入口 |

## 2026-09-09 创作演奏与循环变化

| 能力 | 验收行为 |
| --- | --- |
| Lead 律动 | 创作页独立选择跟随整体、Straight、Swing、Shuffle、Pocket 或 Laid-back；改变 Lead 律动不重写伴奏事件，TAB／五线谱保留直写节奏，播放与点选采用同一律动映射 |
| 复音织体 | 单音、双音点缀、和声 Voicing 三档；附加音必须来自起音所在和弦、使用不同琴弦和合法弦品，Voicing 最多增加两个声部，并在播放、TAB、MusicXML 和 MIDI 中成为真正的同时发声音符 |
| 每轮乐句 | 一次创作按 4 轮 progression 展开，然后回到第 1 轮；可固定重复、按种子随机切换写法，或在 4 个选择器中指定顺序。指定顺序默认“问答 → 动机发展 → 留白 → 留白” |
| 随机约束 | 乐句随机与密度随机使用相互独立的确定性随机流；同种子重现同一计划，随机乐句避免相邻两轮及第 4／第 1 轮接缝重复。固定重复＋固定密度＋单音必须继续得到旧的同一句循环结果 |
| 每轮密度 | 可保持当前音符密度，也可在每轮按种子切换。选择稀疏／适中／密集／繁密作为基准时，随机范围限制在该档与相邻档；“自动”作为基准时可在四个固定密度间选择。变化必须反映在实际起音数量与时值，不只更改标签 |
| 查看与连续播放 | 启用轮间变化后，停播时可选择查看第 1–4 轮 TAB／五线谱，点选后从该轮对应位置试听；播放时谱面自动跟随当前轮并锁定手动轮次选择。相关编辑继续沿用资源准备与下一安全小节线提交 |
| 收藏兼容 | 新收藏保存 v3 完整 4 轮 `leadCycle`、顺序、律动、织体及密度模式；v1 生成记录与 v2 单句快照继续可读取，缺失新字段按“固定重复／固定密度／跟随整体／单音”恢复，不改旧收藏内容 |
| MIDI 范围 | MIDI 仍只导出 Lead。固定重复＋固定密度导出一轮；启用乐句或密度变化时导出完整 4 轮。双音／Voicing 的所有和弦头、所选律动、力度、微时差及拨弦错开都会写入；采样处理、房间声、细微音高漂移和伴奏声部不写入 MIDI |
| Lead 音色 | 保留暖净音／亮净音／厚过载、同日多风格交付的干净短奏／空间延音、大钢琴与小提琴，并增加爵士琴颈、Blues 边缘破音、歌唱延音，共 10 种；三个新增吉他处理都复用现有 CC0 Shinyguitar 录音，不宣称新增吉他录音 |
| Studio 感 | 各轨（包括流派专用声部）加入种子可复现的轻微起音、力度与可定音声部音高偏差，Lead 复音略微错开；分轨设置克制声像与房间发送，修正采样尾音处理。不得将这些程序化处理描述为真人实录或证明已达到商业 backing track 音质 |

创作和伴奏继续共享同一个 AudioContext、绝对时钟、取消与循环逻辑；新变化不得建立第二个时钟、提前加载大型采样或改变伴奏和声。实现验证、资源预算、设备边界与发布状态见 [演奏变化验收](notes/performance-variation-validation-2026-09-09.md)。真人听感、实体手机／耳机／扬声器、蓝牙延迟与不同浏览器的声像效果仍需实机验收。

## 2026-09-09 原创乐句与伴奏强化

| 能力 | 验收行为 |
| --- | --- |
| 演奏取向 | 创作与伴奏提供均衡会话、问答叙事、和声导航、切分口袋、色彩和声、延音空间六项；这是有种子的规则组合，不使用真人姓名，也不得描述为复刻某位演奏家的身份、录音或专有风格 |
| 乐句塑形 | 取向可改变 Lead 的和弦落点、时值门长、力度呼吸、发音、轻微时值与复音错开；均衡会话保持旧默认输出，非法或缺失取向回退均衡会话，同种子和设置必须可重现 |
| 自动配器 | 取向只为 Auto 声部提供亲和池，流派池仍参与选择；用户手动指定的 Bass、鼓、键盘、节奏吉他、辅助打击或弦乐风格始终优先，不得被取向暗中覆盖 |
| 辅助打击 | 新增独立 `percussion` 轨：None、Auto、Shaker 八分推动、Tambourine 反拍、Clap 二四拍；Natural、Bright、Electro 三种音色复用既有原声／原创电子鼓素材与处理，不新增大型媒体 |
| 弦乐层 | 新增独立 `strings` 轨：None、Auto、长音铺底、脉冲推进、回应留白；Chamber、Warm、Air 三种音色复用既有小提琴采样库，以和弦内声部连接、叠加增益补偿和独立空间发送发声，只在启用且有事件时懒加载 |
| 音频生命周期 | 两轨进入既有分轨总线、绝对 AudioContext 时钟、下一安全小节线切换、owner 取消、暂停淡出和尾音清理；不得新建第二套播放器或让停止后的弦乐／空间尾音无限存活 |
| 兼容与收藏 | 省略新选项的底层编配调用默认关闭新增两轨，原四轨事件不被重写；收藏继续使用 v3 与原存储键，新字段可选，旧 v1/v2/v3 缺失时回退均衡会话、关闭新增轨及默认音色，不迁移或覆盖旧记录 |
| MIDI 与边界 | MIDI 仍只导出 Lead，不加入辅助打击、弦乐或其他伴奏；自动测试可证明事件、映射、调度和取消一致，不能证明已达到真人 backing track 质感，实体手机、耳机／扬声器、蓝牙及音量匹配盲听仍需人工验收 |

本轮验证与发布事实统一记录在 [原创与伴奏强化验收](notes/performer-tracks-validation-2026-09-09.md)。公开交付仍只使用 `main/docs` 与 https://rexrockya.github.io/tuner/ ；根 `npm run build` 属于可选 Worker 构建，不得用于覆盖 Pages 前端。

## 2026-09-09 音质优先的真实录音升级

- 用户明确选择音质优先，接受首次使用音色多加载；网站仅按当前乐句实际音高、力度和录音取样，不下载整套银行。FLAC为主，解码或网络失败时使用同PCM WAV回退；成功解码复用，失败重试不中断旧音乐。
- 新原声吉他使用已经过真实音箱录制的FSBS；净音不再默认串联数字饱和和逐音压缩，取消每个slide固定低65cent上滑与机械揉弦。录音未提供的真实连奏/闷击不作承诺。
- Bass扩到三层力度、四次真实演奏；鼓增加实际轻击、重击和可用RR；Shaker/Tambourine/Clap使用真实对应乐器，不再把镲片或军鼓提速截短代替。
- 新增实录Wurli键盘，作为流派默认键盘；旧风琴与Synth选项明确标为合成。Lead独奏小提琴与伴奏弦乐组用不同真实录音库。大钢琴、乐谱播放器与已有收藏音符不改。
- 保留源SFZ音区、力度、调音、循环点与适用的幅度曲线；演奏输入增益置于可选吉他驱动之前，保护轻重弹响应。混音只按声部统一平衡，不逐样本归一化抹掉真实力度。
- pocket/navigator音长只应用一次取向门长，保留短音和原时钟；新播放路径不再次把muted音长乘.62。
- 新采样模块作为studio的明确延迟依赖；生产页面加载后启用真实录音。单独使用旧音频模块的历史消费者仍可用旧路径，必须另有包含新模块和真实manifest的生产路径测试。
- 下一安全小节线提交、准备失败继续旧乐句、候选取消与暂停尾声清理保留；未备齐录音的候选在替换任何已排队计划之前拒绝。
- 首屏gzip6预算仍为120000B；大型媒体不进入首屏请求。来源、署名、修改说明和逐文件固定提交/哈希随资源公开，发布仍仅main/docs。
