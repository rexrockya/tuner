# 弦音产品与数据规划

## 已确认原则

- 唯一对外网址是 `https://rexrockya.github.io/tuner/`。
- 用户可以用用户名和密码自由注册，不要求邮箱或手机号。
- 不建立“家庭”或“教学小组”这种固定关系，所有多人协作统一抽象为“房间”。
- 房间先用于共享节拍器，未来可以承载乐队排练、公共节拍、乐谱翻页和排练进度。
- 当前阶段已完成房间概念改造、云端账号注册登录，以及按用户保存的 Lick 收藏和掌握进度。排行榜和严格多端同步尚未实现。

## 当前实现状态

- 公开网页已接通云端注册、登录和退出。
- 本地测试账号数据库已删除，本机账号服务已停止，网页不会向本机保存用户名或密码。
- 密码只在云端保存加盐后的安全摘要；浏览器只保存不可由网页读取、关闭浏览器后失效的会话 Cookie。
- 登录用户的 Lick 收藏与掌握状态保存到云端，并按用户隔离；未登录时继续使用原有本地记录。

## 核心对象

### 用户

用户拥有唯一用户名、显示名称和密码。密码只能在服务端保存为安全哈希，不能保存明文。一个用户可以创建或加入多个房间。

### 房间

房间由六位房间码进入。成员关系属于房间，而不是家庭或教学组。初期角色只需要：

- 房主：创建房间、控制节拍器。
- 成员：加入房间、接收房间状态。

未来可以增加管理员、乐手声部和只读观众，但不应把家长、孩子、老师或学生写入底层数据结构。

### 个人练习进度

每条 Lick 的状态按“用户 + Lick”保存，建议包含：

- 是否收藏。
- 掌握状态：未开始、练习中、熟悉、已掌握。
- 当前练习 BPM 和最高稳定 BPM。
- A/B 循环点、个人备注、最后练习时间。

未来乐谱进度沿用同一思路，按“用户 + 乐谱”保存掌握状态、练习位置和最高稳定速度。

## 云端数据

少于十人时，继续使用现有 Cloudflare Worker + D1 方案即可，不需要复杂服务器。第一版建议使用这些数据表：

| 数据 | 用途 |
| --- | --- |
| `users` | 用户名、显示名称、密码哈希、注册时间 |
| `sessions` | 登录状态和过期时间 |
| `rooms` | 房间码、房间名、房主、创建时间、状态 |
| `room_members` | 用户加入了哪些房间及其角色 |
| `lick_progress` | 收藏、掌握状态、BPM、备注、更新时间 |
| `practice_sessions` | 每次练习的时长和练习对象 |
| `metronome_state` | 房间当前拍速、启停、版本号和生效时间 |
| `score_progress` | 预留乐谱掌握状态和个人进度 |
| `room_score_state` | 预留房间当前乐谱和统一翻页位置 |

网页仍从唯一公开网址进入；后端地址只作为内部配置，不出现在产品文案里。

## 排行榜

排行榜从用户进度实时统计，先不单独保存容易失真的总数。可以展示：

- 已掌握 Lick 数量。
- 已掌握乐谱数量。
- 本周练习天数和练习时长。
- 最高稳定 BPM 或近期进步值。

默认只显示用户的显示名称。等真实用户使用后，再决定排行榜是全站公开、仅房间内可见，还是允许用户关闭展示。

## 多端同步演进

当前房间只同步 BPM 和启停命令，各设备收到消息后自行开始节拍，适合早期体验，但不属于严格同拍。

严格同步阶段需要由服务端发送统一的未来生效时间，各设备先校准与服务端的时间差，再在同一时刻开始。若后续加入乐谱翻页，同一份房间状态应同时包含节拍版本、乐谱 ID、页码或小节位置和生效时间。届时可以在 Cloudflare 上增加 Durable Objects 和 WebSocket，D1 继续负责持久保存。

## 推荐实施顺序

1. 房间命名和六位房间码。
2. 用户名密码注册、登录和会话。
3. Lick 收藏及掌握进度云同步，保留本地缓存和断网使用。
4. 用户排行榜和隐私开关。
5. 乐谱数据、个人掌握进度和房间统一翻页。
6. 节拍器与乐谱的严格多端同步。
